<Window x:Class="SimpleTrader.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleTrader.WPF"
        xmlns:controls="clr-namespace:SimpleTrader.WPF.Controls"
        mc:Ignorable="d"
        Title="Simple Trader" Height="450" Width="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0">123</TextBlock>
        <Grid Grid.Row="1">
            <controls:NavigationBar DataContext="{Binding Navigator}"/>
        </Grid>
        <ContentControl Grid.Row="2" Content="{Binding Navigator.CurrentViewModel}"/>
        <TextBlock Grid.Row="3">Row 3    456</TextBlock>




    </Grid>
</Window>
